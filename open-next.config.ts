import { defineCloudflareConfig } from '@opennextjs/cloudflare'

export default defineCloudflareConfig({
  // Keep the initial scaffold minimal. Add R2-backed incremental cache
  // once the bucket binding is provisioned in a later stage.
})
